# Streamlit App Requirements
streamlit>=1.28.0
requests>=2.31.0
python-dotenv>=1.0.0

# Backend dependencies (for local development only)
# Comment these out if deploying ONLY the Streamlit frontend
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
